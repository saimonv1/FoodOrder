import classes from './MenuComponent.module.css';

const MenuComponent = () => {
    return (
        <div className={classes.menu}>
            <h1>Menu</h1>
        </div>
    );
};

export default MenuComponent;